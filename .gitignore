.venv/
__pycache__/
*.pyc
.pytest_cache/
.DS_Store
gui_settings.json
*.log
pipeline_run.pid

# Исходники и нарезка — сотни МБ; держите локально или бэкапьте отдельно
audio_files/
segmented_output/

# PyInstaller (audio app)
audio/build/
audio/dist/

# Локальный show QLab (большой bundle)
qlab_playlist/Untitled Workspace/
audio/qlab_playlist/Untitled Workspace/

# Survey app (Node / Vite)
survey-app/**/node_modules/
survey-app/**/dist/
survey-app/backend/function-bundle.zip
survey-app/pg-migrate-backup.dump
survey-app/pg-migrate-no-photo.dump
survey-app/pg-neon-to-yandex-temp.dump
survey-app/pg-neon-migrate.dump
survey-app/frontend/.env
survey-app/frontend/.env.*
!survey-app/frontend/.env.example
!survey-app/frontend/.env.production.example

# Локальные URL бакета / API (не в публичный репозиторий)
survey-app/deploy.config.json
survey-app/.youware.json
survey-app/.env.cloud-function
